# YakYak Server

A simple Node.js server for the YakYak application.

## Running the Server

To start the server, run:

```bash
node index.js
```

The server will start and listen on the configured port.

## Prerequisites

- Node.js installed on your system
- Required dependencies installed via `npm install`
